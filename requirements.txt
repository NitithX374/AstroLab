# AstroLab CLI — Runtime Dependencies
# No external packages needed for core simulation.
 
# Core simulation — uses Python stdlib only (math, dataclasses, json, cmd)
 
# ── Optional: Visualization ─────────────────────────────────────────
# Install these for the 'visualize' command and 'simulate visualize=...'
plotly              # 3D interactive HTML orbit charts
vispy               # Real-time OpenGL 3D visualization
pyqt5               # Qt backend for Vispy (Windows/Linux)
# pyopengl          # Required on some systems for OpenGL support
 
# ── Other Optional ──────────────────────────────────────────────────
# numpy             # Optimization (future)
# astropy           # Advanced units (future)
# rich              # Terminal formatting (future)
